
// Generating random integer using Math.random() and Math.ceil() methods

// Get a random decimal number between 0 and .99 and multiplying it with 10
var decimal = Math.random() * 10;

// rounding off the decimal number
var rounded = Math.ceil(decimal);

console.log(rounded);